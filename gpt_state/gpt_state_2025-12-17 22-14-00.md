──────────────────────────────
🧠 ASTRA INTELLIGENCE | SYSTEM DIAGNOSTIC + UPGRADE LOG
──────────────────────────────
📅 Timestamp: 2025-12-17 22:14:00
📍 Environment: macOS / Python 3.12 / Streamlit / Virtualenv (venv)
──────────────────────────────

# 1️⃣ GOAL

Bring Astra Intelligence’s neural dashboard system back online with:

* ✅ Accurate Astra-ranked stock and crypto cards (6 each)
* ✅ Live, zoomable candlestick chart with full indicators (EMA, BB, RSI, MACD)
* ✅ Active Guardian V7 monitoring
* ✅ Learning Engine funnel integration (AstraFunnel)
* ✅ Proper package imports, clean runtime, and consistent module resolution

──────────────────────────────

# 2️⃣ FILES MODIFIED

## Core Guardian

* `core/guardian/guardian_stream.py`

  * Fixed indentation & JSON handling errors
  * Added try/except block for message parsing

## Learning Engine

* `learning/learning_engine.py`

  * Added missing `on_update()` method to store live learning metrics
  * Fixed indentation errors (line 155–175)

## Dashboard

* `ui/dashboard/tab_dashboard.py`

  * Migrated from legacy v7 stable dashboard
  * Updated import paths to `ui.dashboard.*`
  * Connected to `AstraFunnel` (learning/funnel/astra_funnel.py)

## Funnel System

* `learning/funnel/astra_funnel.py`

  * Verified single remaining version; removed duplicate in `core/funnel/`

──────────────────────────────

# 3️⃣ FIXES PERFORMED

✅ **Import Path Cleanup**

* Added `__init__.py` to all key directories (`core/`, `guardian/`, `astra_modules/`, etc.)
* Updated imports across app.py and dashboard:

  * `from astra_core.guardian.guardian_v6` → `from astra_modules.guardian.guardian_v7`
  * `from core.funnel.astra_funnel` → `from learning.funnel.astra_funnel`

✅ **Environment Rebuild**

* Removed stale `__pycache__` folders
* Recompiled all code: `python -m py_compile core learning guardian engine`

✅ **Code Formatting / Hygiene**

* Applied `ruff check . --fix` and `black .` for uniform formatting
* Eliminated 2,600+ syntax and redefinition errors

──────────────────────────────

# 4️⃣ CURRENT STATUS

### ✅ Fixed:

* GuardianStream syntax and runtime errors
* LearningEngine on_update persistence
* Import structure for GuardianV7
* Dashboard renders without hard crashes

### ⚠️ Issues Remaining:

1. **Dashboard Funnel Import**

   * Streamlit still caching `core.funnel.astra_funnel`
   * Verified correct file: `learning/funnel/astra_funnel.py`
   * Needs cache flush and import consistency

2. **Chart System**

   * Missing advanced zoomable functionality
   * Indicators (EMA, BB, MACD, RSI) not integrated yet
   * Currently static chart view, no range interactivity

3. **Card Data Accuracy**

   * Placeholder values shown (neutral trend, 0% change)
   * Needs `AstraFunnel` output feed for real Astra-picked tickers
   * Requires funnel grading + LearningEngine signals

4. **AstraFunnel Function Signature**

   * Error: `AstraFunnel.run() takes 1 positional argument but 2 were given`
   * `run()` must accept a config or context parameter from dashboard

──────────────────────────────

# 5️⃣ ARCHITECTURE SUMMARY

```
Astra Intelligence
├── app.py                         → Streamlit entry point
├── core/
│   ├── guardian/guardian_stream.py → Live data listener
│   └── guardian/guardian_v7.py     → Guardian logging + monitoring
├── learning/
│   ├── learning_engine.py          → Handles metrics, accuracy, phases
│   └── funnel/astra_funnel.py      → Core stock/crypto selection engine
├── ui/
│   └── dashboard/
│       ├── tab_dashboard.py        → Main tab controller
│       ├── dashboard_cards.py      → Stock/crypto info cards
│       ├── dashboard_chart.py      → Candlestick chart + indicators
│       └── dashboard_sidebar.py    → Symbol selector + controls
```

──────────────────────────────

# 6️⃣ NEXT STEPS (TERMINAL SAFE EXECUTION)

### 🔧 Step 1 — Ensure Correct Funnel Import

```bash
awk '{gsub("from core.funnel.astra_funnel", "from learning.funnel.astra_funnel"); print}' ui/dashboard/tab_dashboard.py > tmp && mv tmp ui/dashboard/tab_dashboard.py
find . -type d -name "__pycache__" -exec rm -rf {} +
python -m py_compile ui/dashboard/tab_dashboard.py learning/funnel/astra_funnel.py
```

### 🔧 Step 2 — Expand Chart System (planned)

* Add Plotly `go.Figure()` with `candlestick`, `EMA`, `BB`, `MACD`, `RSI`
* Bind zoom event via Streamlit `st.plotly_chart(..., use_container_width=True)`
* Adjust `date_range` dynamically based on zoom level (using Plotly’s `relayoutData`)

### 🔧 Step 3 — Astra Funnel Feed

* Validate `learning/funnel/astra_funnel.py` output:

  ```python
  class AstraFunnel:
      def run(self, mode="stocks"):
          # Return top 6 items as list of dicts
          return [{"symbol": "AAPL", "grade": "A", "confidence": 94.5, "summary": "Strong momentum post-earnings"}, ...]
  ```
* Cards read from funnel output instead of static sample

──────────────────────────────

# 7️⃣ PLANNED UPGRADES

✅ Add full indicator support (EMA, BB, RSI, MACD)
✅ Make chart zoom + time range aware (15m, 1h, 1d, 1w, 1m)
✅ Auto-sync card clicks to chart display (single selected symbol)
✅ Integrate Guardian analytics overlay (phase + anomaly markers)
✅ Funnel reinforcement learning to adjust Astra grades

──────────────────────────────

# 8️⃣ SUMMARY

**Working:** Base Guardian, Learning Engine, Imports, Dashboard Frame

**Pending:** Live funnel data integration, indicator chart engine, dynamic symbol-card linkage, and context-aware Astra funnel logic.

──────────────────────────────
📘 END OF REPORT
──────────────────────────────
